= render 'observations/build/form'
.errors

.well
  = form_for [@observation,@observation.photos.build], remote: true do |f|
    .file_upload
      Upload Photo
      = f.file_field :data

#attached_photos    
  = render partial: 'photos/photo', collection: @observation.photos, as: 'photo'