%h1 Observations
%table#obslist
  %thead
    %tr
      %th Date
      %th Primary Observer
      %th Actions
  %tbody
    - @observations.each do |o|
      %tr.observation
        %td= o.obs_datetime
        %td= o.primary_observer.first_and_last_name
        %td= link_to "[edit]", edit_observation_path(o)
:javascript
  $('table#obslist').dataTable({
    "bPaginate": false,
    "bJQueryUI": true
  });